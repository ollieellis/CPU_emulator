#include "instructions.hpp"


void Instructions::execute(uint32_t instruction)
{
    //deduce op code
    // bunch of if statements
    //deduce function and call it


    //if the instruction could not be deduced, throw error
}